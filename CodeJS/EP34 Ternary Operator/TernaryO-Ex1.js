var age = 19;
var canDrive;

// Chuyển đổi đoạn code dưới đây dùng ternary operator mà không làm thay đổi logic
if (age > 16) {
    canDrive = 'yes';
} else {
    canDrive = 'no';
}

// Viết lại ở đây
result = age > 16 ? 'YES' : 'NO';